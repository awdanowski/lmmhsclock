enum School { meets, startsSoon, isOut }

enum ScheduleType { standard, earlyDismissal, delayedStart, pepRally, holiday, vacation, saturday, sunday }

enum StaticMessage { tooEarly, startOfDay, endOfDay, weekend, holiday, vacation }
